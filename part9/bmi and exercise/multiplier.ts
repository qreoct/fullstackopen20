const multiplicator = (a: number, b: number, printText: string) => {
	console.log(printText, a*b);
}

multiplicator(2,4,"multiplying 2 and 4: ")

multiplicator('string',4,"multiplying string and 4: ")